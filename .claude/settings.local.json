{
  "permissions": {
    "allow": [
      "Bash(python auto_labeling.py --image_dir images --output results)",
      "Bash(pip install:*)",
      "Bash(python -m venv .venv)",
      "Bash(.venv/Scripts/pip install:*)",
      "Bash(.venv/bin/pip install:*)"
    ]
  }
}
